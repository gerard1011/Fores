# Direct runtime dependencies only — pip resolves the rest.
#
# This used to be a full `pip freeze` dump, most of which existed to support
# Streamlit (altair, pydeck, pyarrow, protobuf, GitPython, …). With Streamlit
# gone, a curated list of what this service actually imports is both smaller
# and readable. The pipeline's own dependencies live in
# requirements-pipeline.txt and are not installed into the runtime image.
anthropic==0.120.0
fastapi==0.140.0
python-dotenv==1.2.2
uvicorn[standard]==0.51.0
