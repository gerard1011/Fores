services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Read-only so a bug cannot write to the source database, and so
      # refreshing the data needs no rebuild.
      - ./data:/app/data:ro
